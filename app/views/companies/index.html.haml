-# %p#notice= notice
.row
  .col-sm-12
    %p
      %h3
        Companies


  .col
    = link_to 'New Company', new_company_path, :class => "button btn btn-primary float-right"

%hr

%table.table.table-responsive
  %thead.thead-inverse
    %tr
      %th Titulo
      %th{colspan: "3"}
  %tbody
    - @companies.each do |company|
      %tr
        %td= company.titulo
        %td= link_to 'Show', company
        %td= link_to 'Edit', edit_company_path(company)
        %td= link_to 'Destroy', company, method: :delete, data: { confirm: 'Are you sure?' }
%br/
